# {{ ansible_managed }}
127.0.1.1	{{ hostname }}{% if fqdn_hostname is defined and fqdn_hostname | length > 0 %} {{ fqdn_hostname }}{% endif %}

127.0.0.1	localhost

{% if not (no_ipv6 | default(false) | bool) %}
# The following lines are desirable for IPv6 capable hosts
::1	localhost ip6-localhost ip6-loopback
ff02::1	ip6-allnodes
ff02::2	ip6-allrouters
{% endif %}
